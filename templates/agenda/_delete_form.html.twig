<form method="post" action="{{ path('agenda_delete', {'id': agenda.id}) }}" onsubmit="return confirm('êtes-vous sûr de vouloir supprimer cette annonce');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ agenda.id) }}">
    <button class="btn"><i class="fas fa-trash"></i></button>
</form>
