<div class="list-group"><a
            {% if app.request.attributes.get('_route') == 'agenda_index'%}
                class="list-group-item list-group-item-action active"
            {% else %}
                class="list-group-item list-group-item-action"
            {% endif %}
            href="{{ path('agenda_index') }}">Tous</a></div>

<div class="list-group"><a
            {% if app.request.attributes.get('_route') == 'agenda_en_cours'%}
                class="list-group-item list-group-item-action active"
            {% else %}
                class="list-group-item list-group-item-action"
            {% endif %}
            href="{{ path('agenda_en_cours') }}">En cours</a>
</div>
<div class="list-group"><a
            {% if app.request.attributes.get('_route') == 'agenda_a_venir'%}
                class="list-group-item list-group-item-action active"
            {% else %}
                class="list-group-item list-group-item-action"
            {% endif %}
            href="{{ path('agenda_a_venir') }}">A venir</a>
</div>
<div class="list-group"><a
            {% if app.request.attributes.get('_route') == 'agenda_archives'%}
                class="list-group-item list-group-item-action active"
            {% else %}
                class="list-group-item list-group-item-action"
            {% endif %}
            href="{{ path('agenda_archives') }}">Archivés</a>
</div>
