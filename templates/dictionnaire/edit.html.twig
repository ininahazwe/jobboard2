{% extends 'base.html.twig' %}

{% block title %}Edit Dictionnaire{% endblock %}

{% block body %}
    <h1>Edit Dictionnaire</h1>

    {{ include('dictionnaire/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('dictionnaire_index') }}">back to list</a>

    {{ include('dictionnaire/_delete_form.html.twig') }}
{% endblock %}
