<form method="post" action="{{ path('page_delete', {'id': page.id}) }}" onsubmit="return confirm('êtes-vous sûr de vouloir supprimer cette page?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ page.id) }}">
    <button class="btn"><i class="fas fa-trash"></i></button>
</form>
